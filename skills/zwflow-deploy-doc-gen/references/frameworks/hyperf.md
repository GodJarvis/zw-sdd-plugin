# Hyperf 上线文档差异

## 基础设施类型

- 消息基础设施重点识别 Kafka topic、Redis stream。

## 任务配置

- 常驻任务包括需要启动、重启或新增的消息队列消费者和守护进程。
- 扫描关键字：`amqp:consume`、`kafka:consume`、`redis:consume`、`crontab`、`cli`、`task`、`nohup`、`常驻`、`定时`、`一次性`、`迁移脚本`。
