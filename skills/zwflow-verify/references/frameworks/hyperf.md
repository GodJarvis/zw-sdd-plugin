# Hyperf Verify 差异

- 源码根目录：`app/`。
- PHP 变更范围：发布基线到 `HEAD` 的 `app/` 下 PHP 文件。
- 静态分析：`vendor/bin/phpstan analyse <changed_files>`。
- 测试目录：`test/Cases/`、`test/Integration/`。
- 典型调用方测试：`test/Cases/Service/OrderServiceTest.php`。
