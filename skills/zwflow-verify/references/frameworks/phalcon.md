# Phalcon Verify 差异

- 源码根目录：`library/`、`apps/`。
- PHP 变更范围：发布基线到 `HEAD` 的 `library/` 与 `apps/` 下 PHP 文件。
- 静态分析：`vendor/bin/phpstan analyse <changed_files>`。
- 测试目录：`test/Cases/`、`test/Integration/`。
- 典型调用方测试：`test/Cases/Services/OrderServiceTest.php`。
